// 每次发布必须同时更新说明和源码签名。
// vite.config.js 会校验签名；业务源码变了但这里没更新时，构建会直接失败。
//
// 发布流程：
// 1. 修改业务源码后构建，构建错误会提示新的源码签名；
// 2. 把新签名和“年-月-日-v序号”版本写入 RELEASE_UPDATES 第一条，并在 notes 里写清本次更新内容；
// 3. 更早的版本逐条下移，最多保留 3 个版本，超出即删除。
export const RELEASE_UPDATES = Object.freeze([
  {
    version: '2026年08月28日-版本2',
    signature: '2e0e802540',
    notes: [
      '课程表打开速度优化：OCR 结构恢复、作息识别、批量文字解析、导入冲突规则和识别进度界面全部改为点击对应工具后再加载；普通查看与编辑课程不再下载和解析这些代码，课程表主文件由约 117.5 KB 降至约 99.0 KB。功能与现有课程、导入、识图和作息设置保持一致。',
    ],
  },
  {
    version: '2026年08月28日-版本1',
    signature: '427b16839a',
    notes: [
      '版本号改为“年-月-日-版本序号”格式；数据管理新增数据健康卡片，可查看当前浏览器的数据模块数、体积、占用最大的模块、最近备份及浏览器存储用量。',
      '大数据保存进一步优化：同一时间段内的本地安全副本会合并为一次 IndexedDB 读写事务；待办和清单将完成数、金额、筛选结果等重复扫描合并为共享摘要，批量导入或大列表操作更平稳。',
      '页面切换继续提速：常用轻量页面会在空闲网络预取，并保留最近使用的三个页面实例；课程表的 OCR 引擎、版面解析和本地纠错词典改为选择图片后才加载。首页把待办、账单、考试的重复筛选合并为摘要遍历；新增“流畅优先”自动模式，可在低性能设备关闭高成本壁纸滤镜和动画。',
      '页面流畅度优化：个性化默认打开主题页，不再在点击时读取壁纸数据库；账本把月度统计合并为一次遍历，并在大记录量下使用虚拟列表，显著减少首次打开和筛选后的页面节点。',
      '课程表 OCR 改为“原图快速首轮 → 仅在低置信度或表头不足时复核”：高质量图片不再默认进行增强全图、七列和逐行识别；增强处理移除全尺寸像素循环。确认导入的课程名、老师和地点仅保存在本机词库，后续识别只会采纳唯一且接近的候选。',
      '作息表识图导入全面重构：识别结果先进入独立暂存区并生成“识别结果总览”，一次识别出多组作息时自动匹配已有校区与方案、勾选后一键批量导入；替换采用事务写入（全部成功或全部原状），导入前生成差异预览与原数据快照，支持一键撤销。OCR 漏识别的节次会明确标示“未识别”，不再静默填充默认时间；时间冲突只在同一组作息内部检查，首尾相接不再误判，识别不确定、时间缺失、真实冲突、映射待确认分开展示，并支持“只看异常”和分组详情编辑。',
      '修复应用更新后页面可能停留在旧版本的问题：新版本启用后会自动重新打开应用；检查更新时同时比对服务器版本号，避免误报“已是最新版本”。更新通知只保留最近三个版本的说明。',
      '云同步请求改用 POST 传递访问码并加入请求冷却；备份新增 schema 版本与 SHA-256 校验，恢复会同步更新本地安全副本和待同步状态。PWA 首屏只预缓存应用壳与首页，保留深链接并为低内存设备限制 OCR 图片大小。',
      '云同步升级为强一致协调器：旧 KV 数据首次访问时会自动迁移；同一同步空间的版本比较与写入在服务端原子执行，避免两台设备同时推送时相互覆盖。',
    ],
  },
  {
    signature: 'face7fd903',
    notes: [
      '课程表识别不再推定固定七列：改为依据图片中实际识别到的星期锚点建立横向或纵向区域，锚点不足时会保守地提示确认；作息表默认使用版面感知 OCR，按多层“作息季 × 校区”表头关联各时间列；同时修复短课程名被误作教师名而遗漏的问题。',
    ],
  },
  {
    signature: 'earlier-versions',
    notes: [
      '应用更新改为显示浏览器真实上报的检查、下载、准备与应用阶段；长任务（识图、云同步、备份迁移）显示真实步骤、用时与取消重试。',
      '课表批量导入改为统一的“草稿 → 冲突分析 → 提交”流程；课程表统一校区与作息季规则，自动模式支持跨年与日期冲突检查。',
      '手机版底部导航等宽分栏；首页信息层级重排；新增全局“＋ 记账”入口；更新说明按实际源码版本显示。',
    ],
  },
])

// 对用户展示、version.txt 和更新检测统一使用此版本号；后续同日发布只递增“版本”序号。
export const RELEASE_VERSION = RELEASE_UPDATES[0].version

// 与第一条签名保持一致，交给 vite.config.js 校验源码一致性。
export const RELEASE_SOURCE_SIGNATURE = '2e0e802540'

// 兼容旧引用：当前版本的更新说明。
export const RELEASE_NOTES = RELEASE_UPDATES[0].notes
