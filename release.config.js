// 每次发布必须同时更新说明和源码签名。
// vite.config.js 会校验签名；业务源码变了但这里没更新时，构建会直接失败。
//
// 发布流程：
// 1. 修改业务源码后构建，构建错误会提示新的源码签名；
// 2. 把新签名和“年-月-日-v序号”版本写入 RELEASE_UPDATES 第一条，并在 notes 里写清本次更新内容；
// 3. 更早的版本逐条下移，最多保留 3 个版本，超出即删除。
export const RELEASE_UPDATES = Object.freeze([
  {
    version: '2026年08月28日-版本4',
    signature: '24dc302182',
    notes: [
      '电脑与手机切页流畅度修复：撤销会在 iPhone 首次操作时抢占网络和主线程的全页面预热；复杂页面改为有上限地保留最近实例，返回课程表、账本等页面不再反复完整重建。离开课程表时会停止仍在运行的 OCR，避免后台识别拖慢新页面。',
      '大数据页面进一步提速：本地存储的深层监听移到首帧之后且保留立即退出兜底；课程冲突检测合并为一次索引扫描；账本的排序、月/日统计和常记分析共用缓存，不再反复扫描或克隆全部流水。壁纸切换修复并发读取和重复解码问题，设备内安全副本只写真正变化的最新数据。',
      '电脑与手机云同步更稳更省等待：推送减少一次重复网络校验并继续由服务端原子检查版本；支持刷新云端状态和按安全方向一键同步，本次会话刷新页面后无需重输访问码。修复拉取后立即编辑可能漏记、推送期间新修改可能被清除及大数据加密参数溢出；账本分类与偏好也纳入同步。',
      '同步数据不再因 30 天未推送而自动删除。备份恢复新增文字数据与壁纸的完整回滚，二维码合并不会生成重复账本分类；弹窗关闭或缓存页面切换后也不会继续锁住页面。',
    ],
  },
  {
    version: '2026年08月28日-版本3',
    signature: '62d8e4a7b1',
    notes: [
      'iPhone 流畅度专项优化：自动进入“流畅优先”，移除全屏壁纸的高成本滤镜与动画；页面切换不再保留多个已离开的页面实例，降低 Safari 内存压力。首次读取独立壁纸会让新页面先绘制，避免额外的大图解码阻塞点击；启动阶段的本地安全副本写入延后到空闲时执行。底部导航页面代码会在首屏稳定后依次预热，同时不会在省流量或 2G 网络下预取。',
    ],
  },
  {
    version: '2026年08月28日-版本2',
    signature: '2e0e802540',
    notes: [
      '课程表打开速度优化：OCR 结构恢复、作息识别、批量文字解析、导入冲突规则和识别进度界面全部改为点击对应工具后再加载；普通查看与编辑课程不再下载和解析这些代码，课程表主文件由约 117.5 KB 降至约 99.0 KB。功能与现有课程、导入、识图和作息设置保持一致。',
    ],
  },
])

// 对用户展示、version.txt 和更新检测统一使用此版本号；后续同日发布只递增“版本”序号。
export const RELEASE_VERSION = RELEASE_UPDATES[0].version

// 与第一条签名保持一致，交给 vite.config.js 校验源码一致性。
export const RELEASE_SOURCE_SIGNATURE = '24dc302182'

// 兼容旧引用：当前版本的更新说明。
export const RELEASE_NOTES = RELEASE_UPDATES[0].notes
